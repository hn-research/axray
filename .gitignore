node_modules/
dist/
.DS_Store
*.log
.env
.env.*
.tsbuildinfo
coverage/

# Generated by `vhs cassette.tape` at the repo root — we copy the
# launch artifacts into docs/ once they look right and commit them
# there. The root-level versions are ignored to avoid accidental
# commits of work-in-progress recordings.
/launch.gif
/launch.mp4
